# Local match-running harness scratch: commit the scripts/configs only, never the
# heavy/generated/vendored runtime artifacts below.
.venv/
__pycache__/
*.pyc
*.log

# Binaries / vendored builds / weights / checkpoints (large, not source)
RLBotServer
RLBotSim/
collision_meshes
nexto/nexto-model.pt
pulsar-bot/checkpoint/
pulsar-bot/checkpoint_*/

# Per-decision debug JSONL from the bot (GGL_DEBUG_JSONL; a few MB per match)
pulsar-bot/debug.*.jsonl

# Gap-verification handicap markers (written/cleared by play.sh modes, never committed)
nexto/HANDICAPS
pulsar-bot/HANDICAPS
