# CMake
CMakeLists.txt.user
CMakeCache.txt
CMakeFiles
CMakeScripts
Testing
Makefile
cmake_install.cmake
install_manifest.txt
compile_commands.json
CTestTestfile.cmake
_deps

# Ninja
.ninja*
build.ninja

# Build-related
RocketSim.Dir/
build/
x86/
x64/
Release/
Debug
out/
*.obj
*.lib
*.exe
*.pdb
*.ilk
*.tmp
*.pyd
*.whl

# Arena collision mesh files
*.cmf

# Raw binary files
*.bin

# Visual Studio (not using anything VS-related)
.vs/
*.vcxproj
*.sln
*.filters
*.user
CMakeSettings.json

# Anything starting with three underscores
___*

# ML model files
*.pt
*.PT

# Don't include libtorch if in local dir
libtorch
*.lt

/checkpoints
/wandb

# remote tooling
tools/viz/.venv/
run_logs/
build-viz/

# local build trees and run artifacts
build-steer/
build-viz/
run_logs/

# research/ (offline analysis): the toolkit, reports and results JSON all ship.
# Only the regenerable bulk is excluded - datasets, checkpoint caches, the venv.
research/data/
research/.venv/
research/wandb/
__pycache__/

# Serena MCP tool cache
.serena/

# RocketSimV3 cargo build output
GigaLearnCPP/RLGymCPP/RocketSimV3/target/

# Cluster launch scripts (host-specific); keep them out of git
*.sbatch
